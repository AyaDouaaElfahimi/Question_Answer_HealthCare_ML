blinker==1.9.0
# Une bibliothèque Python utilisée pour gérer les signaux et observer les événements. Souvent utilisée dans Flask pour les extensions.

certifi==2024.8.30
# Fournit un ensemble de certificats racines pour valider les certificats SSL utilisés par les bibliothèques HTTP comme `requests`.

charset-normalizer==3.4.0
# Une bibliothèque pour détecter et normaliser les encodages de caractères dans les chaînes de texte.

click==8.1.7
# Un package utilisé pour créer des interfaces en ligne de commande, souvent utilisé avec Flask.

filelock==3.16.1
# Permet la gestion des verrous sur les fichiers, utile pour empêcher l'accès concurrent à des ressources partagées.

Flask==3.1.0
# Un framework web léger en Python, utilisé pour créer des applications web rapides et modulaires.

fsspec==2024.10.0
# Une interface standard pour interagir avec différents systèmes de fichiers, comme les systèmes locaux ou ceux basés sur le cloud.

huggingface-hub==0.26.3
# Fournit des outils pour interagir avec le hub Hugging Face, qui héberge des modèles, datasets, et pipelines de NLP.

idna==3.10
# Gère l'encodage des noms de domaine internationalisés, nécessaire pour les connexions réseau.

itsdangerous==2.2.0
# Utilisé pour gérer les signatures cryptographiques et les données sensibles, fréquemment intégré dans Flask pour les sessions sécurisées.

Jinja2==3.1.4
# Un moteur de templates rapide et flexible utilisé dans Flask pour générer des HTML dynamiques.

joblib==1.4.2
# Une bibliothèque pour la parallélisation et la persistance des données, souvent utilisée dans les workflows de machine learning.

MarkupSafe==3.0.2
# Protège les applications web contre les attaques XSS en échappant automatiquement les données rendues dans les templates.

mpmath==1.3.0
# Fournit des outils pour effectuer des calculs mathématiques précis, souvent utilisés dans des contextes scientifiques.

networkx==3.4.2
# Une bibliothèque pour la modélisation, l'analyse et la manipulation de graphes et réseaux.

numpy==2.1.3
# Une bibliothèque fondamentale pour le calcul numérique, utilisée pour manipuler des tableaux multidimensionnels et effectuer des opérations mathématiques.

nvidia-cublas-cu12==12.4.5.8
# Fournit des fonctions de calcul optimisées pour les GPU NVIDIA, particulièrement pour l'algèbre linéaire.

nvidia-cuda-cupti-cu12==12.4.127
# Un outil de profilage pour les applications CUDA exécutées sur des GPU NVIDIA.

nvidia-cuda-nvrtc-cu12==12.4.127
# Contient un compilateur runtime pour les applications CUDA.

nvidia-cuda-runtime-cu12==12.4.127
# Fournit les bibliothèques runtime nécessaires pour exécuter les programmes CUDA sur un GPU NVIDIA.

nvidia-cudnn-cu12==9.1.0.70
# Une bibliothèque essentielle pour les réseaux de neurones, optimisée pour les GPU NVIDIA.

nvidia-cufft-cu12==11.2.1.3
# Fournit des routines FFT (Fast Fourier Transform) optimisées pour CUDA.

nvidia-curand-cu12==10.3.5.147
# Fournit des fonctions de génération aléatoire optimisées pour CUDA.

nvidia-cusolver-cu12==11.6.1.9
# Une bibliothèque pour résoudre les systèmes d'équations linéaires et d'autres opérations liées à l'algèbre linéaire.

nvidia-cusparse-cu12==12.3.1.170
# Fournit des fonctions pour manipuler les matrices creuses, optimisées pour CUDA.

nvidia-nccl-cu12==2.21.5
# Une bibliothèque de communication collective utilisée pour les architectures multi-GPU.

nvidia-nvjitlink-cu12==12.4.127
# Fournit un support pour le linking JIT (Just-In-Time) dans CUDA.

nvidia-nvtx-cu12==12.4.127
# Fournit des annotations pour profiler les applications CUDA.

packaging==24.2
# Utilisé pour gérer les versions et les dépendances des packages Python.

pandas==2.2.3
# Une bibliothèque pour la manipulation de données structurées, comme les DataFrames et les séries temporelles.

pillow==11.0.0
# Une bibliothèque pour le traitement d'images, comme leur redimensionnement ou leur conversion de format.

python-dateutil==2.9.0.post0
# Fournit des outils pour manipuler les dates et heures.

pytz==2024.2
# Gère les fuseaux horaires et les conversions entre eux.

PyYAML==6.0.2
# Fournit des outils pour lire et écrire des fichiers YAML, souvent utilisés pour des configurations.

regex==2024.11.6
# Une bibliothèque avancée pour les expressions régulières, offrant des fonctionnalités supplémentaires par rapport au module standard `re`.

requests==2.32.3
# Une bibliothèque HTTP pour effectuer des requêtes réseau de manière simple et efficace.

safetensors==0.4.5
# Un format sécurisé pour stocker les tenseurs en évitant les erreurs de désérialisation malveillantes.

scikit-learn==1.5.2
# Une bibliothèque pour le machine learning, comprenant des algorithmes et des outils pour la modélisation prédictive.

scipy==1.14.1
# Une bibliothèque pour les calculs scientifiques, complémentaire à NumPy.

sentence-transformers==3.3.1
# Fournit des outils pour créer et utiliser des embeddings de phrases avec des modèles transformateurs.

setuptools==75.6.0
# Une bibliothèque de gestion de packages et de distribution pour les projets Python.

six==1.16.0
# Permet de créer un code compatible avec Python 2 et 3.

sympy==1.13.1
# Fournit des outils pour effectuer des calculs mathématiques symboliques.

threadpoolctl==3.5.0
# Permet de gérer les bibliothèques basées sur des threads comme NumPy ou OpenBLAS.

tokenizers==0.20.3
# Fournit des outils rapides pour tokenizer le texte, utilisé dans les modèles NLP.

torch==2.5.1
# PyTorch, une bibliothèque pour le deep learning, supportant GPU et CPU.

tqdm==4.67.1
# Fournit des barres de progression pour les boucles et les processus.

transformers==4.46.3
# Une bibliothèque pour les modèles NLP basée sur les transformateurs, développée par Hugging Face.

triton==3.1.0
# Fournit un support pour compiler des kernels spécifiques au deep learning sur GPU.

typing_extensions==4.12.2
# Fournit des fonctionnalités supplémentaires pour les annotations de type Python.

tzdata==2024.2
# Une base de données des fuseaux horaires mise à jour.

urllib3==2.2.3
# Une bibliothèque HTTP pour gérer les connexions et les requêtes.

Werkzeug==3.1.3
# Un utilitaire WSGI pour construire des applications web, souvent utilisé par Flask.
